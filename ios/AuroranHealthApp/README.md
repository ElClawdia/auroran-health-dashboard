# AuroranHealth iOS App

Open `AuroranHealthApp.xcodeproj` in Xcode and run on a simulator or device.

If you need to change the backend URL, edit:
- `AuroranHealthApp/ContentView.swift`

This project uses a `WKWebView` wrapper and persists session cookies by default.
